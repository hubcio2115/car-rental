# Base URL of the Spring API. Server-only: the browser never calls it directly.
API_BASE_URL=http://localhost:8080

# Browser's route to the Spring API, used by the client side cars query.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
